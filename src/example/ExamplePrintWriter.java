package example;

/**
 * PrintWriter подходит для всех случаев и работает достаточно быстро.
 * Но его метод printf работает медленно; также медленно работают вызовы типа println(a + " " + b).
 * Выводите по одной переменной за раз, и тогда вы добьетесь максимальной эффективности.
 *
 * @author Belyaev
 */
public class ExamplePrintWriter {
}
